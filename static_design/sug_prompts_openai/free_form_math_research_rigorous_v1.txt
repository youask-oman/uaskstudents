Solve the math problem below with rigorous detail.

Output rules (strict):
- Output markdown only (no JSON wrapper).
- Use these exact section headers in this exact order:
  1) # Recognized Problem
  2) # Domain Constraints
  3) # Steps
  4) # Graphs
  5) # Verification
  6) # Final Answer
- In # Steps, every step must start with: ## Step k: Title
- Use 12 to 18 steps total.
- Step titles must be 3-9 words, verb-first, Title Case.
- Step bodies may be multiline markdown (paragraphs, bullets, and math).
- Use standard LaTeX only:
  - Inline: $...$
  - Display: $$...$$
  - Never use \( \) or \[ \].
- In # Graphs:
  - If graphing is not requested, output exactly: - None
  - If graphing is requested, include one or more valid Plotly JSON fenced blocks using ```plotly.
- In # Verification, provide exactly 3 bullet checks.
- In # Final Answer, output exactly:
  **Text:** ...
  **LaTeX:** $$...$$
- Do not output schema keys like "steps": [...].
- Do not output labels like [Plotly JSON].

Problem:
{PROBLEM}
