Solve the math problem below quickly but correctly.

Output rules (strict):
- Output markdown only (no JSON wrapper).
- Use these exact section headers in this exact order:
  1) # Recognized Problem
  2) # Domain Constraints
  3) # Steps
  4) # Graphs
  5) # Verification
  6) # Final Answer
- In # Steps, every step must start with: ## Step k: Title
- Use 6 to 10 steps total.
- Keep step bodies concise; multiline is allowed.
- Use standard LaTeX only:
  - Inline: $...$
  - Display: $$...$$
  - Never use \( \) or \[ \].
- In # Graphs:
  - If graphing is not requested, output exactly: - None
  - If graphing is requested, include valid Plotly JSON in fenced ```plotly blocks.
- In # Verification, include 1 to 2 bullet checks.
- In # Final Answer, output exactly:
  **Text:** ...
  **LaTeX:** $$...$$
- Do not output schema keys like "steps": [...].
- Do not output labels like [Plotly JSON].

Problem:
{PROBLEM}
