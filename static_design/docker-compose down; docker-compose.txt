docker-compose down; docker-compose up -d --build

taskkill /IM uvicorn.exe /F
uvicorn app.main:app --port 9000 --log-level debug

docker compose exec -e APP_ENV=DEV orchestrator python scripts/run_admin_smoke_all.py



npx tsc --noEmit --skipLibCheck 2>&1 | Select-String -Pattern "error" | Select-Object -First 20




docker-compose down
docker compose build
docker compose up -d --force-recreate


docker compose down
docker compose build --no-cache
docker compose up -d



What you should do every deploy (the boring, correct ritual)
docker compose up -d --build --remove-orphans
docker compose exec orchestrator alembic upgrade head
docker compose exec orchestrator alembic current
docker compose exec postgres psql -U uask_user -d uask_db -c "SELECT * FROM alembic_version;"





npm i zod; npm i -D playwright; npx playwright install chromium

# Check database health
curl http://localhost:8000/api/admin/health/db
# View error summary
curl http://localhost:8000/api/admin/health/errors/summary
# List open findings
curl http://localhost:8000/api/admin/health/findings?status=OPEN



$ts = Get-Date -Format "yyyyMMdd_HHmmss"
$cid = docker compose ps -q postgres
$remote = "/tmp/uask_db_$ts.dump"
$local  = ".\uask_db_$ts.dump"

docker compose exec -T postgres pg_dump -U uask_user -Fc -d uask_db -f $remote
docker cp "${cid}:${remote}" $local
docker compose exec -T postgres pg_restore -l $remote > $null

Write-Host "OK: $local"




$ts = Get-Date -Format "yyyyMMdd_HHmmss"
$cid = docker compose ps -q postgres
$remote = "/tmp/uask_db_$ts.sql"
$local  = ".\uask_db_$ts.sql"

docker compose exec -T postgres pg_dump -U uask_user -Fp -d uask_db -f $remote
docker cp "${cid}:${remote}" $local
Select-String -Path $local -Pattern "^-- PostgreSQL database dump" | Out-Null

Write-Host "OK: $local"




stripe login
stripe listen --forward-to http://127.0.0.1:9000/api/v1/stripe/webhook


stripe 

>>   -v ${PWD}\.stripe:/root/.config/stripe `
>>   stripe/stripe-cli:latest login
Your pairing code is: finest-humour-roomy-prompt
This pairing code verifies your authentication with Stripe.
To authenticate with Stripe, please go to: https://dashboard.stripe.com/stripecli/confirm_auth?t=Z3xz6R9y2tpIfWAcNHhvQKAGoX9idGPt
> Done! The Stripe CLI is configured for uask test with account id acct_1SzL2V1jBgqm5egs

Please note: this key will expire after 90 days, at which point you'll need to re-authenticate.
PS E:\uaskstudents> 


>>   -v ${PWD}\.stripe:/root/.config/stripe `
>>   stripe/stripe-cli:latest login
Your pairing code is: finest-humour-roomy-prompt
This pairing code verifies your authentication with Stripe.
To authenticate with Stripe, please go to: https://dashboard.stripe.com/stripecli/confirm_auth?t=Z3xz6R9y2tpIfWAcNHhvQKAGoX9idGPt
> Done! The Stripe CLI is configured for uask test with account id acct_1SzL2V1jBgqm5egs

Please note: this key will expire after 90 days, at which point you'll need to re-authenticate.
PS E:\uaskstudents> docker run --rm -it `
>>   -v ${PWD}\.stripe:/root/.config/stripe `
>>   stripe/stripe-cli:latest listen --forward-to http://host.docker.internal:9000/api/v1/stripe/webhook
> Ready! You are using Stripe API Version [2026-01-28.clover]. Your webhook signing secret is whsec_8640921e9ce76e895a641993e5039e0368a75be608d1ee32600dec4e697adb4b (^C to quit)



4242 4242 4242 4242

delete all pyc files 

Get-ChildItem -Recurse -Force -Directory -Filter "__pycache__" | Remove-Item -Recurse -Force
Get-ChildItem -Recurse -Force -File -Filter "*.pyc" | Remove-Item -Force



