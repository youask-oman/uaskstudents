"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var _a, _b, _c, _d, _e;
Object.defineProperty(exports, "__esModule", { value: true });
exports.HoverRegion = exports.SpeechRegion = exports.LiveRegion = exports.ToolTip = exports.StringRegion = exports.DummyRegion = exports.AbstractRegion = void 0;
var StyleJson_js_1 = require("../../util/StyleJson.js");
var SpeechUtil_js_1 = require("../speech/SpeechUtil.js");
var AbstractRegion = (function () {
    function AbstractRegion(document) {
        this.document = document;
        this.CLASS = this.constructor;
        this.AddStyles();
    }
    Object.defineProperty(AbstractRegion, "sheetId", {
        get: function () {
            return 'MJX-' + this.name + '-styles';
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AbstractRegion, "styleSheet", {
        get: function () {
            return document.head.querySelector('#' + this.sheetId);
        },
        enumerable: false,
        configurable: true
    });
    AbstractRegion.prototype.AddStyles = function () {
        var id = this.CLASS.sheetId;
        if (!this.CLASS.style ||
            this.document.adaptor.head().querySelector('#' + id)) {
            return;
        }
        var node = this.document.adaptor.node('style', { id: id });
        node.innerHTML = this.CLASS.style.cssText;
        this.document.adaptor.head().appendChild(node);
    };
    AbstractRegion.prototype.AddElement = function () {
        if (this.div)
            return;
        var element = this.document.adaptor.node('div');
        element.classList.add(this.CLASS.className);
        this.div = element;
        this.inner = this.document.adaptor.node('div');
        this.div.appendChild(this.inner);
        this.document.adaptor
            .body(this.document.adaptor.document)
            .appendChild(this.div);
    };
    AbstractRegion.prototype.Show = function (node) {
        this.AddElement();
        this.position(node);
        this.div.classList.add(this.CLASS.className + '_Show');
    };
    AbstractRegion.prototype.Hide = function () {
        if (!this.div)
            return;
        this.div.remove();
        this.div = null;
        this.inner = null;
    };
    AbstractRegion.prototype.stackRegions = function (node) {
        var rect = node.getBoundingClientRect();
        var baseBottom = 0;
        var baseLeft = Number.POSITIVE_INFINITY;
        var regions = this.document.adaptor.document.getElementsByClassName(this.CLASS.className + '_Show');
        for (var i = 0, region = void 0; (region = regions[i]); i++) {
            if (region !== this.div) {
                baseBottom = Math.max(region.getBoundingClientRect().bottom, baseBottom);
                baseLeft = Math.min(region.getBoundingClientRect().left, baseLeft);
            }
        }
        var bot = (baseBottom ? baseBottom : rect.bottom + 10) + window.scrollY;
        var left = (baseLeft < Number.POSITIVE_INFINITY ? baseLeft : rect.left) +
            window.scrollX;
        this.div.style.top = bot + 'px';
        this.div.style.left = left + 'px';
    };
    return AbstractRegion;
}());
exports.AbstractRegion = AbstractRegion;
var DummyRegion = (function (_super) {
    __extends(DummyRegion, _super);
    function DummyRegion() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    DummyRegion.prototype.Clear = function () { };
    DummyRegion.prototype.Update = function () { };
    DummyRegion.prototype.Hide = function () { };
    DummyRegion.prototype.Show = function () { };
    DummyRegion.prototype.AddElement = function () { };
    DummyRegion.prototype.AddStyles = function () { };
    DummyRegion.prototype.position = function () { };
    return DummyRegion;
}(AbstractRegion));
exports.DummyRegion = DummyRegion;
var StringRegion = (function (_super) {
    __extends(StringRegion, _super);
    function StringRegion() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    StringRegion.prototype.Clear = function () {
        if (!this.div)
            return;
        this.Update('');
        this.inner.style.top = '';
        this.inner.style.backgroundColor = '';
    };
    StringRegion.prototype.Update = function (speech) {
        if (speech) {
            this.AddElement();
        }
        if (this.inner) {
            this.inner.textContent = '';
            this.inner.textContent = speech || '\u00a0';
        }
    };
    StringRegion.prototype.position = function (node) {
        this.stackRegions(node);
    };
    return StringRegion;
}(AbstractRegion));
exports.StringRegion = StringRegion;
var ToolTip = (function (_super) {
    __extends(ToolTip, _super);
    function ToolTip() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    ToolTip.className = 'MJX_ToolTip';
    ToolTip.style = new StyleJson_js_1.StyleJsonSheet((_a = {},
        _a[".".concat(ToolTip.className)] = {
            width: 'auto',
            height: 'auto',
            opacity: 1,
            'text-align': 'center',
            'border-radius': '4px',
            padding: 0,
            'border-bottom': '1px dotted black',
            position: 'absolute',
            display: 'inline-block',
            'background-color': 'white',
            'z-index': 202,
        },
        _a[".".concat(ToolTip.className, " > div")] = {
            'border-radius': 'inherit',
            padding: '0 2px',
        },
        _a['@media (prefers-color-scheme: dark)'] = (_b = {},
            _b['.' + ToolTip.className] = {
                'background-color': '#222025',
                'box-shadow': '0px 5px 20px #000',
                border: '1px solid #7C7C7C',
            },
            _b),
        _a));
    return ToolTip;
}(StringRegion));
exports.ToolTip = ToolTip;
var LiveRegion = (function (_super) {
    __extends(LiveRegion, _super);
    function LiveRegion() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    LiveRegion.setColor = function (type, priority, color, opacity) {
        var style = this.styleSheet;
        if (style) {
            var css = style.sheet.cssRules[0].style;
            var alpha = opacity === 1 ? 1 : "var(--mjx-".concat(type).concat(priority, "-alpha)");
            var name_1 = "--mjx-".concat(type).concat(priority, "-color");
            var value = "rgba(var(--mjx-".concat(type, "-").concat(color, "), ").concat(alpha, ")");
            if (css.getPropertyValue(name_1) !== value) {
                css.setProperty(name_1, value);
            }
            var oname = "--mjx-".concat(type).concat(priority, "-alpha");
            if (css.getPropertyValue(oname) !== String(opacity)) {
                css.setProperty(oname, opacity);
                style.sheet.cssRules[1].cssRules[0].style.setProperty(oname, Math.pow(opacity, 0.7071));
            }
        }
    };
    LiveRegion.className = 'MJX_LiveRegion';
    LiveRegion.priority = {
        primary: 1,
        secondary: 2,
    };
    LiveRegion.style = new StyleJson_js_1.StyleJsonSheet((_c = {
            ':root': {
                '--mjx-fg-red': '255, 0, 0',
                '--mjx-fg-green': '0, 255, 0',
                '--mjx-fg-blue': '0, 0, 255',
                '--mjx-fg-yellow': '255, 255, 0',
                '--mjx-fg-cyan': '0, 255, 255',
                '--mjx-fg-magenta': '255, 0, 255',
                '--mjx-fg-white': '255, 255, 255',
                '--mjx-fg-black': '0, 0, 0',
                '--mjx-bg-red': '255, 0, 0',
                '--mjx-bg-green': '0, 255, 0',
                '--mjx-bg-blue': '0, 0, 255',
                '--mjx-bg-yellow': '255, 255, 0',
                '--mjx-bg-cyan': '0, 255, 255',
                '--mjx-bg-magenta': '255, 0, 255',
                '--mjx-bg-white': '255, 255, 255',
                '--mjx-bg-black': '0, 0, 0',
                '--mjx-live-bg-color': 'white',
                '--mjx-live-shadow-color': '#888',
                '--mjx-live-border-color': '#CCCCCC',
                '--mjx-bg1-color': 'rgba(var(--mjx-bg-blue), var(--mjx-bg-alpha))',
                '--mjx-fg1-color': 'rgba(var(--mjx-fg-black), 1)',
                '--mjx-bg2-color': 'rgba(var(--mjx-bg-red), 1)',
                '--mjx-fg2-color': 'rgba(var(--mjx-fg-black), 1)',
                '--mjx-bg1-alpha': 0.2,
                '--mjx-fg1-alpha': 1,
                '--mjx-bg2-alpha': 1,
                '--mjx-fg2-alpha': 1,
            },
            '@media (prefers-color-scheme: dark)': {
                ':root': {
                    '--mjx-bg-blue': '132, 132, 255',
                    '--mjx-bg-white': '0, 0, 0',
                    '--mjx-bg-black': '255, 255, 255',
                    '--mjx-fg-white': '0, 0, 0',
                    '--mjx-fg-black': '255, 255, 255',
                    '--mjx-live-bg-color': '#222025',
                    '--mjx-live-shadow-color': 'black',
                    '--mjx-live-border-color': '#7C7C7C',
                    '--mjx-bg1-alpha': 0.3,
                    '--mjx-fg1-alpha': 1,
                    '--mjx-bg2-alpha': 1,
                    '--mjx-fg2-alpha': 1,
                },
            }
        },
        _c[".".concat(LiveRegion.className)] = {
            position: 'absolute',
            top: 0,
            display: 'none',
            width: 'auto',
            height: 'auto',
            padding: 0,
            opacity: 1,
            'z-index': '202',
            left: 0,
            right: 0,
            margin: '0 auto',
            'background-color': 'var(--mjx-live-bg-color)',
            'box-shadow': '0px 5px 20px var(--mjx-live-shadow-color)',
            border: '2px solid var(--mjx-live-border-color)',
        },
        _c[".".concat(LiveRegion.className, "_Show")] = {
            display: 'block',
        },
        _c[".".concat(LiveRegion.className, " > div")] = {
            color: 'var(--mjx-fg1-color)',
            'background-color': 'var(--mjx-bg1-color)',
        },
        _c['mjx-container [data-sre-highlight-1]:not([data-mjx-collapsed], rect)'] = {
            color: 'var(--mjx-fg1-color) ! important',
            fill: 'var(--mjx-fg1-color) ! important',
        },
        _c[[
            'mjx-container:not([data-mjx-clone-container])',
            '[data-sre-highlight-1]:not([data-sre-enclosed], rect)',
        ].join(' ')] = {
            'background-color': 'var(--mjx-bg1-color) ! important',
        },
        _c['mjx-container rect[data-sre-highlight-1]:not([data-sre-enclosed])'] = {
            fill: 'var(--mjx-bg1-color) ! important',
        },
        _c['mjx-container [data-sre-highlight-2]'] = {
            color: 'var(--mjx-fg2-color) ! important',
            'background-color': 'var(--mjx-bg2-color) ! important',
            fill: 'var(--mjx-fg2-color) ! important',
        },
        _c['mjx-container rect[data-sre-highlight-2]'] = {
            fill: 'var(--mjx-bg2-color) ! important',
        },
        _c));
    return LiveRegion;
}(StringRegion));
exports.LiveRegion = LiveRegion;
var SpeechRegion = (function (_super) {
    __extends(SpeechRegion, _super);
    function SpeechRegion() {
        var _this = _super.apply(this, __spreadArray([], __read(arguments), false)) || this;
        _this.active = false;
        _this.node = null;
        _this.clear = false;
        _this.voiceRequest = false;
        _this.voiceCancelled = false;
        return _this;
    }
    SpeechRegion.prototype.Show = function (node) {
        _super.prototype.Update.call(this, '\u00a0');
        this.node = node;
        _super.prototype.Show.call(this, node);
    };
    SpeechRegion.prototype.Update = function (speech) {
        var _this = this;
        if (this.voiceRequest) {
            this.makeVoice(speech);
            return;
        }
        speechSynthesis.onvoiceschanged = (function () { return (_this.voiceRequest = true); }).bind(this);
        var promise = new Promise(function (resolve) {
            setTimeout(function () {
                if (_this.voiceRequest) {
                    resolve(true);
                }
                else {
                    setTimeout(function () {
                        _this.voiceRequest = true;
                        resolve(true);
                    }, 100);
                }
            }, 100);
        });
        promise.then(function () { return _this.makeVoice(speech); });
    };
    SpeechRegion.prototype.makeVoice = function (speech) {
        this.active =
            this.document.options.a11y.voicing &&
                !!speechSynthesis.getVoices().length;
        speechSynthesis.cancel();
        this.clear = true;
        var _a = __read((0, SpeechUtil_js_1.buildSpeech)(speech, this.document.options.sre.locale, this.document.options.sre.rate), 2), text = _a[0], ssml = _a[1];
        _super.prototype.Update.call(this, text);
        if (this.active && text) {
            this.makeUtterances(ssml, this.document.options.sre.locale);
        }
    };
    SpeechRegion.prototype.makeUtterances = function (ssml, locale) {
        var e_1, _a;
        var _this = this;
        this.voiceCancelled = false;
        var utterance = null;
        var _loop_1 = function (utter) {
            if (utter.mark) {
                if (!utterance) {
                    this_1.highlightNode(utter.mark, true);
                    return "continue";
                }
                utterance.addEventListener('end', function (_event) {
                    if (!_this.voiceCancelled) {
                        _this.highlightNode(utter.mark);
                    }
                });
                return "continue";
            }
            if (utter.pause) {
                var time_1 = parseInt(utter.pause.match(/^[0-9]+/)[0]);
                if (isNaN(time_1) || !utterance) {
                    return "continue";
                }
                utterance.addEventListener('end', function (_event) {
                    speechSynthesis.pause();
                    setTimeout(function () {
                        speechSynthesis.resume();
                    }, time_1);
                });
                return "continue";
            }
            utterance = new SpeechSynthesisUtterance(utter.text);
            if (utter.rate) {
                utterance.rate = utter.rate;
            }
            if (utter.pitch) {
                utterance.pitch = utter.pitch;
            }
            utterance.lang = locale;
            speechSynthesis.speak(utterance);
        };
        var this_1 = this;
        try {
            for (var ssml_1 = __values(ssml), ssml_1_1 = ssml_1.next(); !ssml_1_1.done; ssml_1_1 = ssml_1.next()) {
                var utter = ssml_1_1.value;
                _loop_1(utter);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (ssml_1_1 && !ssml_1_1.done && (_a = ssml_1.return)) _a.call(ssml_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        if (utterance) {
            utterance.addEventListener('end', function (_event) {
                _this.highlighter.unhighlight();
            });
        }
    };
    SpeechRegion.prototype.Hide = function () {
        this.cancelVoice();
        _super.prototype.Hide.call(this);
    };
    SpeechRegion.prototype.cancelVoice = function () {
        this.voiceCancelled = true;
        speechSynthesis.cancel();
        this.highlighter.unhighlight();
    };
    SpeechRegion.prototype.highlightNode = function (id, init) {
        if (init === void 0) { init = false; }
        this.highlighter.unhighlight();
        var nodes = Array.from(this.node.querySelectorAll("[data-semantic-id=\"".concat(id, "\"]")));
        if (!this.clear || init) {
            this.highlighter.highlight(nodes);
        }
        this.clear = false;
    };
    SpeechRegion.style = null;
    return SpeechRegion;
}(LiveRegion));
exports.SpeechRegion = SpeechRegion;
var HoverRegion = (function (_super) {
    __extends(HoverRegion, _super);
    function HoverRegion() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    HoverRegion.prototype.position = function (node) {
        var nodeRect = node.getBoundingClientRect();
        var divRect = this.div.getBoundingClientRect();
        var xCenter = nodeRect.left + nodeRect.width / 2;
        var left = xCenter - divRect.width / 2;
        left = left < 0 ? 0 : left;
        left = left + window.scrollX;
        var top;
        switch (this.document.options.a11y.align) {
            case 'top':
                top = nodeRect.top - divRect.height - 10;
                break;
            case 'bottom':
                top = nodeRect.bottom + 10;
                break;
            case 'center':
            default: {
                var yCenter = nodeRect.top + nodeRect.height / 2;
                top = yCenter - divRect.height / 2;
            }
        }
        top = top + window.scrollY;
        top = top < 0 ? 0 : top;
        this.div.style.top = top + 'px';
        this.div.style.left = left + 'px';
    };
    HoverRegion.prototype.Show = function (node) {
        this.AddElement();
        this.div.style.fontSize = this.document.options.a11y.magnify;
        this.Update(node);
        _super.prototype.Show.call(this, node);
    };
    HoverRegion.prototype.Clear = function () {
        if (!this.div)
            return;
        this.inner.textContent = '';
        this.inner.style.top = '';
        this.inner.style.backgroundColor = '';
    };
    HoverRegion.prototype.Update = function (node) {
        if (!this.div)
            return;
        this.Clear();
        var mjx = this.cloneNode(node);
        var selected = mjx.querySelector('[data-mjx-clone]');
        this.inner.style.backgroundColor = node.style.backgroundColor;
        selected.style.backgroundColor = '';
        selected.classList.remove('mjx-selected');
        this.inner.appendChild(mjx);
        this.position(node);
    };
    HoverRegion.prototype.cloneNode = function (node) {
        var mjx = node.cloneNode(true);
        mjx.setAttribute('data-mjx-clone', 'true');
        if (mjx.nodeName !== 'MJX-CONTAINER') {
            if (mjx.nodeName !== 'g') {
                mjx.style.marginLeft = mjx.style.marginRight = '0';
            }
            var container = node.closest('mjx-container');
            if (mjx.nodeName !== 'MJX-MATH' && mjx.nodeName !== 'svg') {
                var math = container.firstChild;
                if (math.nodeName === 'MJX-BBOX') {
                    math = math.nextSibling;
                }
                mjx = math.cloneNode(false).appendChild(mjx).parentElement;
                var enclosed = Array.from(container.querySelectorAll('[data-sre-enclosed]'));
                math.nodeName === 'svg'
                    ? this.svgClone(node, enclosed, mjx, container)
                    : this.chtmlClone(node, enclosed, mjx);
            }
            mjx = container.cloneNode(false).appendChild(mjx).parentElement;
            mjx.style.margin = '0';
            mjx.style.minWidth = '';
        }
        mjx.setAttribute('data-mjx-clone-container', 'true');
        return mjx;
    };
    HoverRegion.prototype.chtmlClone = function (node, enclosed, mjx) {
        var e_2, _a;
        try {
            for (var enclosed_1 = __values(enclosed), enclosed_1_1 = enclosed_1.next(); !enclosed_1_1.done; enclosed_1_1 = enclosed_1.next()) {
                var child = enclosed_1_1.value;
                if (child !== node) {
                    var id = child.getAttribute('data-semantic-id');
                    if (!id || !mjx.querySelector("[data-semantic-id=\"".concat(id, "\"]"))) {
                        mjx.appendChild(child.cloneNode(true));
                    }
                }
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (enclosed_1_1 && !enclosed_1_1.done && (_a = enclosed_1.return)) _a.call(enclosed_1);
            }
            finally { if (e_2) throw e_2.error; }
        }
    };
    HoverRegion.prototype.svgClone = function (node, enclosed, mjx, container) {
        var e_3, _a;
        var _b;
        var _c = node.getBBox(), x = _c.x, y = _c.y, width = _c.width, height = _c.height;
        if (enclosed.length) {
            mjx.firstChild.remove();
            var g_1 = container.querySelector('g').cloneNode(false);
            try {
                for (var enclosed_2 = __values(enclosed), enclosed_2_1 = enclosed_2.next(); !enclosed_2_1.done; enclosed_2_1 = enclosed_2.next()) {
                    var child = enclosed_2_1.value;
                    var clone = g_1.appendChild(child.cloneNode(true));
                    if (child === node) {
                        clone.setAttribute('data-mjx-clone', 'true');
                    }
                    var _d = __read(this.xy(child), 2), cx = _d[0], cy = _d[1];
                    clone.setAttribute('transform', "translate(".concat(cx, ", ").concat(cy, ")"));
                }
            }
            catch (e_3_1) { e_3 = { error: e_3_1 }; }
            finally {
                try {
                    if (enclosed_2_1 && !enclosed_2_1.done && (_a = enclosed_2.return)) _a.call(enclosed_2);
                }
                finally { if (e_3) throw e_3.error; }
            }
            mjx.appendChild(g_1);
            var rect = node.previousSibling;
            var bbox = rect.getBBox();
            width = bbox.width;
            height = bbox.height;
            var _e = __read(this.xy(rect), 2), X = _e[0], Y = _e[1];
            x = X;
            y = Y + bbox.y;
        }
        var g = container.querySelector('g');
        if (container.getAttribute('width') === 'full' &&
            g.firstChild.lastChild === node) {
            mjx.innerHTML = '';
            mjx.appendChild(container.cloneNode(true).firstChild);
            mjx.querySelector('.mjx-selected').setAttribute('data-mjx-clone', 'true');
            (_b = mjx.querySelector('[data-sre-highlighter-added]')) === null || _b === void 0 ? void 0 : _b.remove();
            return;
        }
        mjx.firstChild.setAttribute('transform', 'scale(1, -1)');
        var W = parseFloat((mjx.getAttribute('viewBox') || mjx.getAttribute('data-mjx-viewBox')).split(/ /)[2]);
        var w = parseFloat(mjx.style.minWidth || mjx.getAttribute('width'));
        mjx.setAttribute('viewBox', [x, -(y + height), width, height].join(' '));
        mjx.removeAttribute('style');
        mjx.setAttribute('width', (w / W) * width + 'ex');
        mjx.setAttribute('height', (w / W) * height + 'ex');
    };
    HoverRegion.prototype.xy = function (node) {
        var P = DOMPoint.fromPoint({ x: 0, y: 0 }).matrixTransform(node.getCTM().inverse());
        return [-P.x, -P.y];
    };
    HoverRegion.className = 'MJX_HoverRegion';
    HoverRegion.style = new StyleJson_js_1.StyleJsonSheet((_d = {},
        _d[".".concat(HoverRegion.className)] = {
            display: 'block',
            position: 'absolute',
            width: 'max-content',
            height: 'auto',
            padding: 0,
            opacity: 1,
            'z-index': '202',
            margin: '0 auto',
            'background-color': 'white',
            'line-height': 0,
            'box-shadow': '0px 10px 20px #888',
            border: '2px solid #CCCCCC',
        },
        _d[".".concat(HoverRegion.className, " > div")] = {
            overflow: 'hidden',
            color: 'var(--mjx-fg1-color)',
            'background-color': 'var(--mjx-bg1-color)',
        },
        _d['@media (prefers-color-scheme: dark)'] = (_e = {},
            _e['.' + HoverRegion.className] = {
                'background-color': '#222025',
                'box-shadow': '0px 5px 20px #000',
                border: '1px solid #7C7C7C',
            },
            _e),
        _d['mjx-container[data-mjx-clone-container]'] = {
            padding: '2px ! important',
        },
        _d['mjx-math > mjx-mlabeledtr'] = {
            display: 'inline-block',
            'margin-right': '.5em ! important',
        },
        _d['mjx-math > mjx-mtd'] = {
            float: 'right',
        },
        _d));
    return HoverRegion;
}(AbstractRegion));
exports.HoverRegion = HoverRegion;
//# sourceMappingURL=Region.js.map