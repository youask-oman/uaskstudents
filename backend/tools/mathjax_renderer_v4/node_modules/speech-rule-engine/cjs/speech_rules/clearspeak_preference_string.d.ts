import { AxisMap, DynamicProperties } from '../rule_engine/dynamic_cstr.js';
export declare const PREFERENCES: DynamicProperties;
export declare function fromPreference(pref: string): AxisMap;
export declare function toPreference(pref: AxisMap): string;
export declare function findPreference(prefs: string, kind: string): string;
export declare function addPreference(prefs: string, kind: string, value: string): string;
