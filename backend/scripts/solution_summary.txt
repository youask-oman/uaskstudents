Problem: Find and classify the critical points of f(x) = x^3 - 3x + ln(x) on its domain.
Tier: RESEARCH
----------------------------------------
Assumptions: Domain is x>0 because of ln(x); however f and its derivative can be considered for x>0. Note: f' equation multiplied by x to solve cubic assumes x\ne 0 which is outside domain, so valid for domain.

Steps:
1. State domain and differentiate
   The function includes ln(x) so its natural domain is x>0. Compute the first derivative using standard differentiation rules: derivative of x^3 is 3x^2, of -3x is -3, and of ln(x) is 1/x. This derivative is valid for x>0.
   Latex: f(x)=x^3-3x+\ln(x) \text{Domain: }x>0 f'(x)=3x^2-3+\frac{1}{x}

2. Set derivative to zero to find critical points
   Critical points occur where f'(x)=0 (interior points) or derivative undefined. On domain x>0 the derivative is defined, so solve f'(x)=0. Multiply the equation by x>0 to clear the denominator; multiplying by a positive number preserves equality and sign of roots.
   Latex: f'(x)=0\iff 3x^2-3+\frac{1}{x}=0 \text{Multiply by }x:\;3x^3-3x+1=0

3. Analyze the cubic for number of real roots
   Consider g(x)=3x^3-3x+1, a cubic with positive leading coefficient, so g(x)\to -\infty as x\to -\infty and g(x)\to +\infty as x\to +\infty implying at least one real root. Compute g'(x) to detect local extrema of g and thus possibility of three real roots.
   Latex: g(x)=3x^3-3x+1 g'(x)=9x^2-3=3(3x^2-1) g'(x)=0\iff x=\pm\frac{1}{\sqrt{3}}

4. Locate sign changes of g to bracket roots
   Evaluate g at sample points to find intervals where g changes sign; each sign change on an interval indicates a real root in that interval by the Intermediate Value Theorem. We only keep roots with x>0 because domain requires x>0; still we note negative-root behavior for completeness of algebraic solution when multiplying by x earlier doesn't introduce extraneous positive roots.
   Latex: g(-2)=-24+6+1=-17 g(-1)= -3+3+1=1 g(0)=1 g(0.4)=3(0.064)-1.2+1=0.192-1.2+1=-0.008 g(0.75)=3(0.421875)-2.25+1=1.265625-2.25+1=0.015625

5. Conclude cubic has three real roots and identify positive ones
   From the sign evaluations g(-2)<0<g(-1) there is a negative root; from g(0)>0 and g(0.4)<0 there is a root in (0,0.4); from g(0.4)<0 and g(0.75)>0 there is a root in (0.4,0.75). Because the original domain restricts to x>0, only the two positive roots in these intervals are valid critical points for f. Multiplying by x did not introduce spurious positive roots because x=0 is not in domain.
   Latex: \text{Roots: one in }(-2,-1),\;\text{one in }(0,0.4),\;\text{one in }(0.4,0.75) \text{Keep only positive roots for }f\text{: two positive roots in }(0,0.4)\text{ and }(0.4,0.75)

6. Obtain numerical approximations of positive critical points
   Use a standard numeric root-finding (Newton or bisection) on g(x)=0 restricted to x>0. Bisection is robust given the tested sign changes; provide approximations to three decimal places for clarity and later second-derivative evaluation.
   Latex: \text{Solve }3x^3-3x+1=0\text{ on }(0,\infty) \text{Positive roots approximately }x_1\approx 0.397,\;x_2\approx 0.744

7. Compute second derivative for classification
   Compute f''(x) to use the second-derivative test at each critical point. Ensure expression is valid on domain x>0 (denominator nonzero). The sign of f'' at a critical point determines local min (positive) or max (negative).
   Latex: f''(x)=6x-\frac{1}{x^2} \text{Domain: }x>0\Rightarrow f''\text{ defined}

8. Evaluate f'' at the smaller positive critical point and classify
   Substitute x\approx 0.397 into f'' to determine concavity. Because x>0 the arithmetic is straightforward; a negative f'' indicates local concave down and thus a local maximum. This is the standard second-derivative test. Numerical substitution gives a clear sign.
   Latex: x\approx 0.397 f''(0.397)=6(0.397)-\frac{1}{(0.397)^2}\approx 2.382-6.344\approx -3.962<0 \text{Conclusion: local maximum at }x\approx 0.397

9. Evaluate f'' at the larger positive critical point and classify
   Substitute x\approx 0.744 into f'': a positive value indicates local concave up and therefore a local minimum by the second-derivative test. Provide the numeric result to justify the sign.
   Latex: x\approx 0.744 f''(0.744)=6(0.744)-\frac{1}{(0.744)^2}\approx 4.464-1.808\approx 2.656>0 \text{Conclusion: local minimum at }x\approx 0.744

10. Sanity check via sign of f' around critical points
   Verify classification by checking sign changes of f' immediately left and right of each positive critical point: for a local max f' should go + to -, for a local min - to +. Evaluate f' at sample points: at x=0.3, f'(0.3)=3(0.09)-3+3.333\approx0.27-3+3.333=0.603>0; at x=0.5, f'(0.5)=0.75-3+2= -0.25<0; at x=0.6, f'(0.6)=1.08-3+1.667=-0.253<0; at x=0.8, f'(0.8)=1.92-3+1.25=0.17>0. These sign changes match the earlier classifications: f' goes + to - across x\approx0.397 (local max) and - to + across x\approx0.744 (local min).
   Latex: f'(0.3)=3(0.09)-3+\frac{1}{0.3}\approx0.27-3+3.333\approx0.603>0 f'(0.5)=3(0.25)-3+2=0.75-3+2=-0.25<0 f'(0.8)=3(0.64)-3+1.25=1.92-3+1.25\approx0.17>0

11. Report critical points with classification and note about negative root
   Summarize the valid critical points on the domain x>0 with their classifications and mention the third real root of the cubic is negative thus extraneous for f because ln(x) excludes nonpositive x.
   Latex: \text{Critical points on domain }x>0:\;x\approx 0.397\text{ (local max)},\;x\approx 0.744\text{ (local min)} \text{Cubic also has a negative root }x\approx -1.214\text{ but }x\le 0\text{ is not in domain}

Final Answer: On the domain x>0 f has two critical points: x≈0.397 (local maximum) and x≈0.744 (local minimum).
Latex: \text{Critical points: }x\approx 0.397\;\text{(local max)},\;x\approx 0.744\;\text{(local min)}
